import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary
  static const Color primary = Color(0xFF0061a4);
  static const Color onPrimary = Colors.white;
  static const Color primaryContainer = Color(0xFFd1e4ff);
  static const Color onPrimaryContainer = Color(0xFF001d36);

  // Backgrounds
  static const Color background = Color(0xFFFEF9F2); // Tailwind bg-background
  static const Color surface = Colors.white;
  
  // Text
  static const Color textPrimary = Color(0xFF1a1c1e);
  static const Color textSecondary = Color(0xFF404752);
  static const Color textTertiary = Color(0xFF707883);

  // Borders
  static const Color border = Color(0xFFbfc7d4);

  // Status / Semantic
  static const Color success = Color(0xFF006e1c);
  static const Color error = Color(0xFFba1a1a);
  static const Color warning = Color(0xFFf57f17);
  static const Color info = Color(0xFF0288d1);
}
